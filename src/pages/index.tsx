import { Box } from "@chakra-ui/react";
import CRAForm from "../features/cra-form";

export default function Home() {
  return (
    <Box maxW={["100%", "100%", "90%"]}>
      <CRAForm />;
    </Box>
  );
}
